#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int g;

	for (g = 0; g <= 9; g++)
	{
		printf("%d", g);
	}
	printf("\n");
	return (0);
}
