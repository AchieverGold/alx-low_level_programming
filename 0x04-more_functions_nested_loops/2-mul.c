#include "main.h"

/**
 * mul - Multiples two numbers
 * @x: the first number to multiple
 * @y: the second number to multiply with the first
 * Return: The result of multiplication between x num and y num
 */

int mul(int x, int y);
{
return (x * y);
}
